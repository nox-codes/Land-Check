{
  "name": "landcheck",
  "version": "1.0.0",
  "description": "LandCheck – Land title verification platform",
  "private": true,
  "scripts": {
    "dev": "concurrently -n server,studio,client -c cyan.bold,magenta.bold,yellow.bold --kill-others-on-fail \"npm run dev --prefix server\" \"npm run db:studio --prefix server\" \"npm run dev --prefix client\"",
    "build": "npm run build --prefix server && npm run build --prefix client",
    "start": "npm run start --prefix server",
    "db:migrate": "npm run db:migrate --prefix server",
    "db:generate": "npm run db:generate --prefix server",
    "db:studio": "npm run db:studio --prefix server",
    "install:all": "npm install --prefix server && npm install --prefix client"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
